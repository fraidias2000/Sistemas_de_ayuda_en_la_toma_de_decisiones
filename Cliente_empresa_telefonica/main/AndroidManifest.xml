<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.empresa_telefonica">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VER_BONOS"></activity>
        <activity android:name=".TERMINOS_Y_CONDICIONES" />
        <activity android:name=".DESPEDIDA" />
        <activity android:name=".CALCULAR_AHORRO" />
        <activity android:name=".VER_TARIFAS" />
        <activity android:name=".MENU_INICIO" />
        <activity android:name=".PANTALLA_PRINCIPAL">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>